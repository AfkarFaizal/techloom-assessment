# Copy this file to .env and fill in your MongoDB Atlas connection string.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/?retryWrites=true&w=majority

# Optional: database name (defaults to ecommerce_shop if not set)
MONGODB_DB_NAME=ecommerce_shop

# Optional: port the server listens on (defaults to 4002)
PORT=4002
